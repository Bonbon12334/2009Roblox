// user.js: Dynamically sets the username (this can be changed later to fetch real user data)
const username = "RandomUser"; // This is static for now
document.getElementById('username').textContent = username;
